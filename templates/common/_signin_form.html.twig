
<fieldset>
    <legend><i class="fa fa-lock"></i> {{ 'title.user_create'|trans }}</legend>

	<form method="get" action="{{ path('user_new') }}">
		<button type="submit" class="btn btn-primary">
			<i class="fa fa-sign-in"></i> {{ 'action.create'|trans }}
		</button>
	</form>
</fieldset>